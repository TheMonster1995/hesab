# Local dev env for the API (Prisma reads DATABASE_URL from here).
# For local dev, point at localhost; in Docker the compose file injects the "db" host.
DATABASE_URL=postgresql://hesab:hesab@localhost:5432/hesab?schema=public
JWT_SECRET=change-me-please-use-a-long-random-string
PORT=4000
